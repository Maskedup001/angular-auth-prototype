1. Изучил тему 'Функции в TypeScript'

2. Выполнил домашнее заданеи в котором нужно было создать функцию calculate, 
которая принимает три параметра: два числа и строку-оператор ("+", "-", "*", "/"). 
В зависимости от переданного оператора функция должна возвращать результат операции.

2.1. Дополнительно добавил, если оператор не распознан то выводится строка с ошибкой ("Ошибка: неизвестный оператор"). 
Чтобы избежать ошибки TypeScript (Type 'string' is not assignable to type 'number'), 
я сделал с помощью логического опреатора | - ИЛИ, так что бы функция возвращала число и строку 